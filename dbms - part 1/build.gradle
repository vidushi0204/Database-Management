/*
 * This build file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java Library project to get you started.
 * For more details take a look at the Java Libraries chapter in the Gradle
 * user guide available at https://docs.gradle.org/4.4.1/userguide/java_library_plugin.html
 */

// Apply the java-library plugin to add support for Java Library
apply plugin: 'java-library'
apply plugin: 'java'

// In this section you declare where to find the dependencies of your project
repositories {
    // Use jcenter for resolving your dependencies.
    // You can declare any Maven/Ivy/file repository here.
    jcenter()
    mavenCentral()
}

sourceSets {
    main {
        java {
            srcDirs 'in/ac/iitd/src/main/java'
        }
        resources {
            srcDirs 'in/ac/iitd/src/main/resources'
        }
    }
    test {
        java {
            srcDirs 'in/ac/iitd/src/test/java'
        }
    }
}

dependencies {
    // This dependency is exported to consumers, that is to say found on their compile classpath.
    api 'org.apache.commons:commons-math3:3.6.1'

    compile 'org.apache.calcite:calcite-core:1.32.0'
    compile 'org.apache.calcite:calcite-linq4j:1.32.0'
    compile 'org.apache.calcite:calcite-file:1.32.0'

    compile 'org.slf4j:slf4j-api:2.0.9'
    compile 'org.slf4j:slf4j-simple:2.0.9'

    // hamcrest
    // compile 'org.hamcrest:hamcrest-all:1.3'

    // This dependency is used internally, and not exposed to consumers on their own compile classpath.
    implementation 'com.google.guava:guava:23.0'

    // Use JUnit test framework
    testImplementation 'junit:junit:4.12'
}